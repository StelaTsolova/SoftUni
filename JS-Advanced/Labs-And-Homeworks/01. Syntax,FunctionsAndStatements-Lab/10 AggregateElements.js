function printOperations(array) {
    let inverseSum = 0;

    for (let index = 0; index < array.length; index++) {
        sum += 1 / array[index];
    }

    console.log(array.reduce((a, b) => a + b));
    console.log(inverseSum);
    console.log(array.join(''));
}
